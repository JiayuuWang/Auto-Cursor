# 基于图形用户界面（GUI）的 ai agent

## 其功能是：

通过操作图形界面，访问google ai studio官网，获取模型推理结果。利用在试用平台调用模型免费这一特征，打造一个“免费”的api大模型提供商

## 具体操作过程是：

1. 用户输入query并选择模型类型
2. agent打开浏览器
3. agent访问google ai studio官网，选择模型类型，在输入框中输入query
4. agent点击运行按钮，等待片刻，获取模型推理结果
5. agent将结果返回给用户